rules_version = '2';
// Only the server (using the Admin SDK) may touch the database.
// Browsers can never read or write it directly, so client data and password hashes stay private.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
